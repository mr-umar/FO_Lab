#include <stdio.h>
#include <stdlib.h>

int main() {
    int pr = sizeof(int); // Almacena el tamaño de un entero en bytes

    printf("El tamaño de un entero en este sistema es: %d bytes\n", pr); // Imprime el valor

    return 0;
}
